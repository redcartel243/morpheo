import {
    Box, Heading, VStack, FormControl, FormLabel, Input, NumberInput, 
    NumberInputField, NumberInputStepper, NumberIncrementStepper, 
    NumberDecrementStepper, Select, ColorPicker, ColorPickerTrigger, 
    ColorPickerPositioner, ColorPickerContent, ColorPickerArea, 
    ColorPickerAreaThumb, ColorPickerChannelInput,
    InputGroup, InputRightElement, Button, Flex, Icon, Tooltip
} from '@chakra-ui/react';
import { FiInfo } from 'react-icons/fi'; 